<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sh.Mapper.EtcMapper">

	<!-- 코드 관련 쿼리 -->
	<select id="foodList" resultType="com.sh.Domain.Food" parameterType="com.sh.Domain.Dto">
		SELECT * FROM SITLIB.FOODTABLE
		WHERE GDATE = #{queryDate} AND GUBN like '%'||#{queryType}||'%'
		ORDER BY GUBN, SER
	</select> 
	
	<select id="selectItDamage" resultType="com.sh.Domain.ItDamage">   
		SELECT * FROM SITLIB.IT_DAMAGE WHERE stat = '01' order by RTIME DESC 
	</select>
	
	<update id="endDamage" parameterType="com.sh.Domain.ItDamage">
		UPDATE SITLIB.IT_MST_HIS2 SET
		ctime = #{ctime} 
		WHERE jtime = #{rtime}
	</update>		 
</mapper>